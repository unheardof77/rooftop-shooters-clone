import { 
    ARM,
    CHARACTER,
    PROJECTILE,
    STAGE
    } from './constants';

export const CHARACTER_SHAPE = { width: CHARACTER.width, height: CHARACTER.height, radius: CHARACTER.radius }; // in physics units
export const ARM_SHAPE = { width: ARM.width, height: ARM.height };
export const PROJECTILE_SHAPE = { radius: PROJECTILE.radius };
export const STAGE_SHAPE = { width: STAGE.width, height: STAGE.height };


